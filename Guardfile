guard 'coffeescript', :output => 'server/lib' do
  watch(%r{server/src/.+\.coffee})
end

guard 'coffeescript', :output => 'client/public/javascripts' do
  watch(%r{client/coffeescripts/.+\.coffee})
end
